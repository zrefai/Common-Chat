import React from "react";
import "./Header.css";
import Logo from "../Logo/Logo";

const Header = () => {
  return (
    <div className="header">
      <Logo size={35} />
    </div>
  );
};

export default Header;
